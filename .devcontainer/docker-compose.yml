version: "3.7"
services:
  app:
    build:
      dockerfile: ./.devcontainer/Dockerfile
      context: ../    
    restart: always
    volumes:
      - "../:/workspace:cached"
    ports:
      - "8000:8000"